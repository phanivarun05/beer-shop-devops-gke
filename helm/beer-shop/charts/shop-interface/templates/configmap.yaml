apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "shop-interface.fullname" . }}
  labels:
    {{- include "shop-interface.labels" . | nindent 4 }}

data:

  config.yaml: |
{{ .Values.configs.application | indent 4 }}

  registry.yaml: |
{{ .Values.configs.registry | indent 4 }}