apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "payment.fullname" . }}
  labels:
    {{- include "payment.labels" . | nindent 4 }}

data:

  config.yaml: |
{{ .Values.configs.application | indent 4 }}

  registry.yaml: |
{{ .Values.configs.registry | indent 4 }}
    